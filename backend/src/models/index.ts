// Exportar todos los modelos desde aquí
export { default as Empresa } from './Empresa.model';

// Aquí se pueden agregar relaciones entre modelos
// Por ejemplo:
// Empresa.hasMany(Producto);
// Producto.belongsTo(Empresa);
